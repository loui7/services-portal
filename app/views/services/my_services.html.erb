<h1>Services</h1>
<% @services.each do |service| %>
    <div class="service-listing">
        <hr>
        <h2><%= link_to service.title, service_path(service.id) %></h2>
        <p><%= service.location %></p>
        <p><%= service.description %></p>
    </div>
<% end %>